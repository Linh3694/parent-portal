import React from 'react';
import { View, Text } from 'react-native';

const Attendance = () => (
    <View className="flex-1 justify-center items-center bg-white">
        <Text className="text-lg font-bold">Trang Điểm danh</Text>
    </View>
);

export default Attendance;
