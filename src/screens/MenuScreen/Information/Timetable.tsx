import React from 'react';
import { View, Text } from 'react-native';

const Timetable = () => (
    <View className="flex-1 justify-center items-center bg-white">
        <Text className="text-lg font-bold">Trang Thời khóa biểu</Text>
    </View>
);

export default Timetable;
