import React from 'react';
import { View, Text } from 'react-native';

const MenuService = () => (
    <View className="flex-1 justify-center items-center bg-white">
        <Text className="text-lg font-bold">Trang Thực đơn</Text>
    </View>
);

export default MenuService; 