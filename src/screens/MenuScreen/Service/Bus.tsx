import React from 'react';
import { View, Text } from 'react-native';

const Bus = () => (
    <View className="flex-1 justify-center items-center bg-white">
        <Text className="text-lg font-bold">Trang Bus</Text>
    </View>
);

export default Bus;
