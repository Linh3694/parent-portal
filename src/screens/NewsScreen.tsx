import React from 'react';
import { View, Text } from 'react-native';

const NewsScreen = () => (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text style={{ fontSize: 22, fontWeight: 'bold' }}>Trang Bảng tin</Text>
    </View>
);

export default NewsScreen;
